<!doctype html>
<html class="js full-height" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#F4F4F4">
    <meta name="robots" content="noindex">

    <link rel="canonical" href="{{ canonical_url }}">

    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    <title>{{ shop.name }}</title>

    <meta name="description" content="{{ page_description | escape }}">

    {% render 'meta-tags' %}

    {{ content_for_header }}

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">

    {% style %}
      :root {
        --color-background: 244, 244, 244;
        --color-foreground: 45, 46, 48;
        --color-button: 100, 163, 157;
        --color-button-text: 255, 255, 255;
        --font-body-family: 'Playfair Display', serif;
        --font-body-style: normal;
        --font-body-weight: 400;
        --font-heading-family: 'Playfair Display', serif;
        --font-heading-style: normal;
        --font-heading-weight: 500;
      }

      body {
        background-color: rgb(var(--color-background));
        color: rgb(var(--color-foreground));
        font-family: var(--font-body-family);
        font-style: var(--font-body-style);
        font-weight: var(--font-body-weight);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        margin: 0;
        padding: 2rem;
        position: relative;
        overflow: hidden;
      }

      body::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(45deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 100%);
        pointer-events: none;
        z-index: 1;
      }

      .password-content {
        position: relative;
        z-index: 2;
        text-align: center;
        max-width: 600px;
        width: 100%;
        animation: fadeIn 1.5s ease-out;
      }

      .password-heading {
        font-family: var(--font-heading-family);
        font-weight: var(--font-heading-weight);
        font-size: 3.5rem;
        line-height: 1.2;
        margin: 0 0 2rem;
        letter-spacing: 0.02em;
      }

      .password-subtext {
        font-size: 1.2rem;
        line-height: 1.6;
        margin-bottom: 3rem;
        opacity: 0.8;
        max-width: 400px;
        margin-left: auto;
        margin-right: auto;
      }

      .password-form {
        margin: 2rem 0;
      }

      .password-input {
        background: transparent;
        border: 1px solid rgba(var(--color-foreground), 0.1);
        border-radius: 0;
        color: rgb(var(--color-foreground));
        font-family: var(--font-body-family);
        font-size: 1rem;
        padding: 1rem;
        width: 100%;
        max-width: 300px;
        margin-bottom: 1rem;
        transition: border-color 0.3s ease;
      }

      .password-input:focus {
        outline: none;
        border-color: rgb(var(--color-foreground));
      }

      .password-button {
        background: rgb(var(--color-button));
        color: rgb(var(--color-button-text));
        border: none;
        padding: 1rem 2rem;
        font-family: var(--font-body-family);
        font-size: 1rem;
        cursor: pointer;
        transition: opacity 0.3s ease;
        width: 100%;
        max-width: 300px;
      }

      .password-button:hover {
        opacity: 0.9;
      }

      .password-email-form {
        margin-top: 3rem;
      }

      .password-email-input {
        background: transparent;
        border: 1px solid rgba(var(--color-foreground), 0.1);
        border-radius: 0;
        color: rgb(var(--color-foreground));
        font-family: var(--font-body-family);
        font-size: 1rem;
        padding: 1rem;
        width: 100%;
        max-width: 300px;
        margin-bottom: 1rem;
        transition: border-color 0.3s ease;
      }

      .password-email-input:focus {
        outline: none;
        border-color: rgb(var(--color-foreground));
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @media screen and (max-width: 749px) {
        .password-heading {
          font-size: 2.5rem;
        }
        
        .password-subtext {
          font-size: 1rem;
        }
      }
    {% endstyle %}
  </head>

  <body class="password">
    <main id="MainContent" class="password-main">
      <div class="password-content">
        <h1 class="password-heading">A New Aura is Coming.</h1>
        <p class="password-subtext">
          We're reimagining Evora for the women who move with softness and strength.<br>
          Be the first to return when the doors open.
        </p>

        <div class="password-form">
          {% form 'storefront_password' %}
            {{ form.errors | default_errors }}
            <input
              type="password"
              name="password"
              id="Password"
              class="password-input"
              placeholder="Enter password"
            >
            <button type="submit" class="password-button">
              Enter
            </button>
          {% endform %}
        </div>

        <div class="password-email-form">
          <p class="password-subtext">Get notified when we launch</p>
          {% form 'customer' %}
            <input
              type="email"
              name="contact[email]"
              id="Email"
              class="password-email-input"
              placeholder="Your email"
              required
            >
            <button type="submit" class="password-button">
              Notify Me
            </button>
          {% endform %}
        </div>
      </div>
    </main>
  </body>
</html>
